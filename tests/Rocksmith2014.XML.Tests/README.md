# Rocksmith2014.XML.Tests

Test data directory for the `rocksmith2014-xml` crate.

Place any `.xml` arrangement test fixtures here. The unit and integration tests
live in `rocksmith2014-xml/tests/xml_tests.rs` and use inline XML strings, so
no external data files are required at this time.
