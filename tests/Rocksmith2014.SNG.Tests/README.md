# Rocksmith2014.SNG.Tests

Test data directory for the `rocksmith2014-sng` crate.

Place any `.sng` binary test fixtures here. The round-trip unit tests live in
`rocksmith2014-sng/src/lib.rs` (inline `#[cfg(test)]` module) and do not
require external data files.
